# Pedir que el usuario ingrese (input) nombre de personas y mostrarlos hasta que el valor
# de lo que ingresa sea “fin”

nombres = str

while(nombres != 'fin'):
    nombres = input('Ingrese nombres de personas: ')